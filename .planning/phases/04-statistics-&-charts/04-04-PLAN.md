---
phase: 04-statistics-&-charts
plan: 04
type: execute
wave: 4
depends_on: ['04-03']
files_modified: []
autonomous: false

must_haves:
  truths:
    - 'Charts render with real data and correct labels'
    - 'Empty states appear when data is insufficient'
    - 'Summary values match expected totals for a sample vehicle'
  artifacts: []
  key_links: []
---

<objective>
Verify the statistics UI renders correctly with real data and empty states.

Purpose: Ensure charts, summary values, and empty states are visually correct and match the data rules.
Output: Human-verified confirmation of Phase 4 UI behavior.
</objective>

<execution_context>
@/home/bweber/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/bweber/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@pages/vehicles/[id]/statistics.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Vehicle statistics page with summary cards, consumption line chart, monthly cost bar chart, and empty states</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Open `/vehicles/{id}/statistics` for a vehicle with at least two full-tank fuelings.
    3. Confirm:
       - Summary shows total PLN spent, average L/100km, and total distance.
       - Consumption chart shows points over time and tooltips display values.
       - Monthly cost chart shows bars with month labels.
    4. Open `/vehicles/{id}/statistics` for a vehicle with fewer than two full-tank fuelings.
    5. Confirm consumption chart and average consumption show a "not enough data" message instead of numbers.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification confirms chart rendering, summaries, and empty states.
</verification>

<success_criteria>

- Charts display correctly for vehicles with valid data
- Insufficient data produces clear, meaningful empty states
  </success_criteria>

<output>
After completion, create `.planning/phases/04-statistics-&-charts/04-04-SUMMARY.md`
</output>
