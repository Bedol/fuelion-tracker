---
phase: 07-flow-wiring-cache-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/vehicles/[id]/index.tsx
  - hooks/useCreateFueling.ts
  - hooks/useUpdateFueling.ts
  - hooks/useDeleteFueling.ts
autonomous: true

must_haves:
  truths:
    - 'Vehicle detail header provides a primary View statistics action'
    - 'Dashboard data refreshes after fueling create, edit, and delete'
  artifacts:
    - path: 'pages/vehicles/[id]/index.tsx'
      provides: 'Header action linking to /vehicles/{id}/statistics'
      contains: 'View statistics'
    - path: 'hooks/useCreateFueling.ts'
      provides: 'Dashboard invalidation on fueling create'
      contains: "queryKey: ['dashboard']"
    - path: 'hooks/useUpdateFueling.ts'
      provides: 'Dashboard invalidation on fueling update'
      contains: "queryKey: ['dashboard']"
    - path: 'hooks/useDeleteFueling.ts'
      provides: 'Dashboard invalidation on fueling delete'
      contains: "queryKey: ['dashboard']"
  key_links:
    - from: 'pages/vehicles/[id]/index.tsx'
      to: 'pages/vehicles/[id]/statistics.tsx'
      via: 'router push to statistics route'
      pattern: '/statistics'
    - from: 'hooks/useCreateFueling.ts'
      to: 'hooks/useDashboardData.ts'
      via: 'queryClient.invalidateQueries'
      pattern: "queryKey: ['dashboard']"
---

<objective>
Wire navigation to vehicle statistics and refresh the dashboard cache after fueling changes.

Purpose: Restore the sign-in → vehicle → fueling → stats → dashboard flow with fresh data.
Output: A clear statistics entry point and dashboard invalidation in fueling mutations.
</objective>

<execution_context>
@/home/bweber/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/bweber/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-flow-wiring-cache-consistency/07-CONTEXT.md

@pages/vehicles/[id]/index.tsx
@pages/vehicles/[id]/statistics.tsx
@hooks/useCreateFueling.ts
@hooks/useUpdateFueling.ts
@hooks/useDeleteFueling.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a primary View statistics action to vehicle details</name>
  <files>pages/vehicles/[id]/index.tsx</files>
  <action>
In the vehicle detail header action area (the ButtonGroup near the top), add a primary button labeled "View statistics" that routes to `/vehicles/${vehicleId}/statistics`.

Ensure "View statistics" is the primary action (solid/primary styling). Adjust the existing "Edit Vehicle" button to a secondary style (outline or similar) so the stats entry stands out. Keep delete/back actions intact.
</action>
<verify>
Open `pages/vehicles/[id]/index.tsx` and confirm the ButtonGroup includes a primary "View statistics" button that navigates to `/vehicles/${vehicleId}/statistics`.
</verify>
<done>Vehicle detail header exposes a clear, primary entry to the statistics page.</done>
</task>

<task type="auto">
  <name>Task 2: Invalidate dashboard cache after fueling mutations</name>
  <files>hooks/useCreateFueling.ts, hooks/useUpdateFueling.ts, hooks/useDeleteFueling.ts</files>
  <action>
In each fueling mutation hook, add dashboard cache invalidation on success:
- `useCreateFueling`: call `queryClient.invalidateQueries({ queryKey: ['dashboard'] })` after existing invalidations.
- `useUpdateFueling`: call `queryClient.invalidateQueries({ queryKey: ['dashboard'] })` after existing invalidations.
- `useDeleteFueling`: call `queryClient.invalidateQueries({ queryKey: ['dashboard'] })` after existing invalidations.

Keep the existing invalidations for fuelings/lastFueling intact.
</action>
<verify>
Open the three fueling hooks and confirm each `onSuccess` includes `queryClient.invalidateQueries({ queryKey: ['dashboard'] })`.
</verify>
<done>Fueling create/edit/delete operations trigger dashboard data refresh.</done>
</task>

</tasks>

<verification>
- Manual smoke: edit or delete a fueling, then return to the dashboard and confirm recent activity updates without a full reload.
</verification>

<success_criteria>

- Vehicle detail provides a primary "View statistics" action in the header.
- Dashboard cache invalidates on fueling create, edit, and delete.
  </success_criteria>

<output>
After completion, create `.planning/phases/07-flow-wiring-cache-consistency/07-02-SUMMARY.md`
</output>
